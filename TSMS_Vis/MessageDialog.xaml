<Window x:Class="TSMS_Vis.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TSMS_Vis"
        mc:Ignorable="d"
        Title="TSMS Error" Height="200" Width="400" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="tbMessage" TextWrapping="WrapWithOverflow" Text="Message" FontSize="16" FontWeight="Bold" Foreground="Red"/>
        </StackPanel>
        <Border Grid.Row="1" Background="Gray" />
        <Button Grid.Row="1" Name="okButton" Content="OK" Width="80" Margin="10" Click="okButton_Click"/>
    </Grid>
</Window>
